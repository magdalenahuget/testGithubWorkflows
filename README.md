The SolarWatch is a Spring Boot Web API project that provides 
the sunrise and sunset times for a given city on a given date, 
in local or UTC times.

Files and fields needed:
- test.properties:
  - api.key
- application.properties:
  - api.key




qwe